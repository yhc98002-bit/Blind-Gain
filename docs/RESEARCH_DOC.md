# Blind Gains — Living Research Document
Maintained in-repo (suggested path: `docs/RESEARCH_DOC.md`). Codex appends to the experiment logs after every closed task; PIs own §1–§3. Nothing here overrides the merged preregistration.

## 1. Thesis and claims

Multimodal RLVR (GRPO-family) is implicitly assumed to improve visual perception. Mechanistically, within a rollout group the image is encoded once and all reward variance comes from text sampling, so credit flows to text-side decisions; if training items are solvable from priors, captions, or format compliance, reward routes around the pixels. **Claim under test:** benchmark gains from standard multimodal RLVR are substantially text-side, and certified visual dependence does not improve.

**RQ1 (decomposition):** what fraction of RLVR gains survives removing visual information at training time (real / gray / no-image / caption arms)?
**RQ2 (certification):** does training move paired counterfactual flip accuracy (FlipTrack) above nulls?
**Positive control (mini-A5):** can RL move the instrument at all? A small CP-GRPO run (joint pair reward) serves as the RL-trainability existence proof — reframed from "repair" to control.

## 2. Paper 1 / Paper 2 charter (scope amendment, 2026-07-12; PI-2 ratification pending)

**Paper 1 — the measurement paper** ("what RLVR actually optimizes when you hand it images"): blind-solvability audit as a dataset property (multi-family, inference-only); decomposition on two corpora (geo3k, ViRL39K) at two scales (3B seeded, 7B flagship); FlipTrack certification with full validity dossier (attackers+CIs, one-shot R20, 72B caption stress, degradation/scale/SFT controls, mini-A5 RL control); G_format accounting identity; q_i reward-opportunity mechanism; robustness arms (unfrozen tower, long horizon). Renderable-only instrument, framed as artifact-immunity by construction; released as instrument+generator protocol (R19+R20+generator).

**Paper 2 — the method paper** (CP-GRPO as repair, at scale): transfer to external benchmarks, 7B, natural-scene counterfactual pairs, reward shapes/annealing, corpus mixtures, caption-grounded reward variants. Depends on Paper 1's instrument. Scoop mitigation: Paper 1 states the repair direction explicitly as open, timestamping the framing.

**Resist-list (bloat guard):** no extra benchmarks beyond the seven; no new eval templates beyond certified set + doc-vNext; no 32B/72B training (72B = inference stress tool only); no multi-recipe/framework sweeps; no Paper-2 transfer questions inside Paper 1's A5 section.

## 3. Standing design decisions (consolidated; chat-ratified)

Measurement: canonical-v2 parser (agreement-vs-native threshold retired — all residuals must be canonical-favorable with taxonomy); extractor_valid vs contract_valid split; Acc_strict = contract_valid ∧ Acc_final; StrictGain = AnswerGain + G_format (exact identity, fixture-enforced); final-answer-span scoring with tiered matching + ambiguity guard; within-template key-shuffle null; SESOI ±0.05 with paired-CI equivalence language.
Training: decomposition arms freeze the vision tower; anchor = recipe-reproduction arm (native reward, recipe settings, untouched); pilot reward = canonical-v2 extraction → mathruler precedence, 0.5/0.5 weights (evidence vs native pending), itimer 5s guard, native shadow logging; filtered corpus only (frozen ID hash); matched steps, report tokens/wall-clock, no FLOP claims.
Instrument: R20 no-iteration rule (failures downgrade, never regenerate); caption gates at main scale + 72B stress; per-category endpoints (geometry primary, overall secondary, document calibration-only due to 7B saturation); contamination language = "conservative candidates."
Process: preregistration merges before first optimizer step, no peeking; gates computed as AND, PI-declared only; every fix ships an adversarial fixture; honest `blocked` is always acceptable; ledger + goal mechanism per round.
Ops: single-node placement always at our scale; TP no wider than the model needs (TP1 replicas for ≤7B); GPUs grabbed opportunistically, researcher's jobs are normal neighbors; utilization is a report, not a gate; storage = shared-save → login-archive sweep, latest-raw retention, 20 GiB quota floor / guards as backstops; CP-GRPO implementation (Paper-1 mini-A5 and Paper 2) = shared group uid + broadcast pair reward (≡ pair-level normalization), advantage-tensor equivalence test required.

## 4. Completed experiments (headline numbers)

| # | Experiment | Result | Status/caveat |
|---|---|---|---|
| E1 | Recipe anchor, 3B geo3k GRPO, 100 steps | geo3k test 0.1498→0.4359 (+0.2862, CI [+0.2446,+0.3278]); FlipTrack R19 overall +0.0017 [−0.0183,+0.0209], geometry +0.0083 [−0.0183,+0.0367]; gray/noise 0.0000, collapse 1.0 | Prior observation disclosed in revised prereg. Unfiltered corpus (contains test-near conservative candidates); native reward; vision tower unfrozen vs frozen pilot A1 |
| E2 | FlipTrack R19 (1,200 pairs: doc 300/geo 600/chart 300) | 3B real 0.87/0.47/0.44; 7B caption ≤0.06; gray/noise 0; degradation monotone; scale control and attacker gates pass; Richard accepted 60/60 human-audit pairs | Frozen; no R19 edits. v07 chart construct relabeled “cued point-value reading”; circle bypasses legend→series hop, caption is inaccurate, nine-series color separation marginal |
| E3 | R20 one-shot confirmatory (fresh seeds, no iteration) | document: generator-pass; geometry 0.397 / chart 0.390 3B-real → miss the ≥0.40 band only; ALL validity criteria pass (caption ≤0.012@7B, floor 0, monotone) | geometry/chart certification = "R19-selected"; disclosed in prereg |
| E4 | 72B caption stress | R19 pair acc 0.0533; R20 0.0617 | Strongest validity evidence; weights hash-deleted (146.8 GB) |
| E5 | Blind-solvability audit v2 (pilot contract: 2048 tok, pilot reward, filtered 1,288 train + 601 test, 5 conditions × 16 samples) | greedy: real 0.1535, **caption 0.1747 (> real)**, gray 0.0651, noise 0.0588, none 0.0503; mean q_i: real 0.363, caption 0.374, gray 0.220, none 0.218; ≥75% of gray/none items at 0/16 floor | Prereg anchor numbers. v1 (512-tok) superseded, retained |
| E6 | Layer-1 base tables, 3B+7B, 7 suites + blind variants | e.g. 3B MMStar 0.554→0.261 blind; MathVista 0.624→0.329 blind | Harness pinned; local judge; G_format fields |
| E7 | Decontamination | geo3k↔Layer-1: 463 candidates; + train-vs-test → filtered subset 1,288/2,101, SHA 8631d015… | "Conservative candidates" language mandatory |
| E8 | Parser/reward infrastructure | agreement audit v2 = 0.9156, all 27 residuals canonical-favorable (native defects); pilot-reward 5-step smoke: 13,401 rows, shadows valid, no NaN; disagreements ~2% under mathruler precedence | Native r1v lines 45/49 establish 0.5 accuracy + 0.5 format; launched anchor has no kwargs override; pilot matches native |
| E9 | Doc v-next calibration (one-shot 100 pairs) | 3B real 0.69; 7B real 1.00 → too easy | No iteration; harder family = Paper-2/later |
| E10 | Ops/governance | storage guards+retention+quota tooling; goal-loop caught 3 Goodhart patterns (gate-status conjunction, duplicate audited file, registry authorship) — all structurally patched | Methodology section material |
| E11 | ViRL39K blind-solvability, 4,096 items, five conditions | mean q_i: real 0.5115, caption 0.4355, gray 0.4188, noise 0.4251, no-image 0.4151; all 15 independent audit checks pass | Registered fork selects strong source/category heterogeneity; H-mixed headline and stratified readouts |
| E12 | Paper-1 artifact pipeline | section/result/data-card/nonoverlap registries plus immutable builders for decomposition bars, hurdle intervals, dissociation scatter, and audit tables | V5 audit passes after registered methods, corpus manifests, and landed audit hashes were populated; future scientific result slots remain explicit and fail closed |
| E13 | M4 extension authorization | Long-horizon four-way terminal rule, fixed step 400, benchmark context condition, M8-driven four-arm 7B template, and ViRL stratification amendment are merged | M5 authorized after restore integrity; M6/M7/M9 retain their independent readiness requirements |
| E14 | 7B ViRL39K blind-solvability, 4,096 items, five conditions | greedy real 0.3579, gray 0.2456, noise 0.2483, no-image 0.1824, caption 0.1624; mean q_i 0.5356/0.4424/0.4439/0.4144/0.4458; all 15 audit checks pass | M8 inference-only readiness result. Precommitted gray-vs-no-image rule retains A2 in the 7B flagship; audit-sample caption store is not the future training-subset store |
| E15 | Chart-v08 human-audit software | Package-detected fixed 700x450 rendering; viewer zoom and original-image links disabled; six standard checks plus no-zoom-correct and series-unambiguous; 13 focused tests pass | Software-ready only. Human outcome, strong-caption scoring, interpretation, freeze, and confirmation remain pending |
| E16 | Registered four-arm Geometry3K pilot, seed 1 | `Acc_final` gain: A1 +0.2529 [0.2097,0.2945], A2 gray +0.0200 [-0.0017,0.0416], A2b no-image +0.0300 [0.0083,0.0516], A3 caption +0.1098 [0.0732,0.1448]; recovery fractions 0.079/0.118/0.434; A1 R19 geometry -0.0017 [-0.0283,0.0250] | Registered one-seed estimands only; item CIs do not measure RL seed variance. StrictGain identities hold; no PI gate decision is made. M10 follow-up is pending for 47/8/7/18 frozen support candidates |

## 5. In flight

- M0: both PIs approved; `reports/preregistration_pilot_v1.md` is final, pins introduction commit `2782815cc057...`, and records merge-as-sign-off before any optimizer step.
- M2: complete operationally. All four registered A1/A2/A2b/A3 runs reached step 100; every 601-row Geometry3K output and R19 step-60/100 lifecycle passed fail-closed identity/hash checks before the single four-arm readout opened values. The report includes registered contrasts, recovery fractions, hurdle/rank analyses, exact StrictGain accounting, all R19 categories, matched optimizer budget, and actual retained tokens/active time. The initial report path typo failed before row loading; a later renderer-only retry used the immutable machine JSON after the frozen geometry category identifier was corrected. PIs retain interpretation authority.
- M8: all five 7B TP1 conditions completed exactly 4,096 rows. The summary and true independent audit are complete: all 15 checks pass, source image/hash and row identities are exact, and score recomputation has zero mismatches. Gray greedy accuracy 0.2456 [0.2327, 0.2595] and no-image 0.1824 [0.1702, 0.1939] fire the precommitted rule retaining A2-gray in the flagship. The question-blind audit-sample store covers all 4,297 unique images; full training-subset captions remain pending.
- M11: the pre-existing marker-only watchdog opened when A2's exact R19 marker landed and launched eight InternVL cells on an29 before the seed-1 report completed. Two ViRL blind cells failed before inference because their input-helper import transitively required the training-only `mathruler` package. Commit `70444e7` separates lightweight input conditioning and passes an adversarial no-mathruler import fixture without changing the frozen M11 environment. A 25-file InternVL stage was hash-verified on an12, where fresh real/no-image/caption 4,096-row cells run TP1 on GPUs 4/5/6. The reconciled queue has advanced to 7 complete / 7 running / 4 pending with `performance_values_opened=false`. The final builder requires verified model-stage evidence for every backend-node placement, including both InternVL copies.
- M12: the declared 100-pair v08 calibration batch remains immutable. Structurally complete, still-uninterpreted lifecycles exist for 3B/7B real, caption, gray, and noise cells, the 7B no-star/random-star necessity diagnostic, and grouped DINOv2/frequency/metadata attackers. The 146.8-GB 72B ModelScope checkout completed and its tree hash is recorded on an12 `/dev/shm`; TP4 caption stress has not started. The package-detected fixed 700x450 viewer now disables viewer zoom and original-image links, adds the two v08 ratings, passes 13 focused tests, and is ready for Richard's post-seed-1 human audit.
- M4: PI ruling R1 is merged as the exact authorization marker. The long-horizon primary and four-way verdict are fixed, step 400 is terminal, and M5 is authorized after restore integrity. R4 fixes the 7B arm set to A1/A2/A2b/A3 by the precommitted audited gray/no-image rule. M6/M7/M9 remain blocked only by their own registered readiness requirements.
- M5/M3: all eight step-100 anchor model/optimizer shards were restored and hash-audited. The step-101 integrity child completed with exit 0 and all eight engineering checks passed; no PI gate was declared. The registered fixed step-400 continuation is active on an12 GPUs 0-3 with terminal-no-extension pinned and both retention and relocation-independent evaluation watchers running. Seed-2 A1 is active on an29 GPUs 2/5/6/7. Two one-hour monitors completed with no fatal signatures or host-memory/swap failure; the second observed M5 reach structural step 102 and A1 reach step 3 while opening no scientific values.
- M13: pipeline delivery is complete and machine-audited; V6 preserves V5 evidence while making the pass-status scope explicit and consistency-safe. The methods appendix and data card pin landed contracts/manifests while future values remain fail-closed slots.
- M10: the support-sharpening protocol and fail-closed adapter are registered. M2 now freezes 47 A1, 8 A2-gray, 7 A2b, and 18 A3 eligible items under each arm's own 0/16 baseline condition. Exactly 64 additional frozen-base samples per item and non-causal wording remain required before any support-expansion label.
- M6: exact CP and matched member-level rewards use the pilot extraction/mathruler precedence, explicit pair identity, and stable rollout indices. Self-review caught the draft control sharing one pair uid across A/B, which would have normalized a mixed `2G` control group; no optimizer step used it. V2 restricts the shared uid to CP and gives each standard-control prompt its own `G=5` group. The immutable training-only corpus contains 3,000 pairs/6,000 rows across three templates; an independent full audit verifies every image and exact changed-pixel mask, Parquet/JSONL identity, A/B adjacency, and zero template/pair/image-hash overlap with R19, R20, and chart-v08. The 192-pair, no-optimizer step-0 audit completed with exact recomputation: CP hit rate/variance 0.1469/0.1253 and member 0.2526/0.1888; named-trajectory CP signal is sparse at 0.0125 and retained without reweighting. A new 300-pair held-out answer-preserving catch set contributes 100 pairs per template; all 18 exhaustive checks pass, including exact masks, unchanged queried pixel regions, preserved answers, random 147/153 side assignment, and zero train/evaluation overlap. The combined advantage/config audit passes 31/31 checks. The CP/member one-step smoke registration passes 11/11 commit-binding checks and a GPU-inert queue now waits behind seed 2 and M11 for a fully free node; no smoke step has run. A separate post-smoke main launch marker remains fail-closed.
- Storage: the explicitly superseded initial A3 and failed initial A1 archives were separately listed, hashed, and preserved on shared storage before their source bytes were removed. The A1 move preserved 39 files / 57,249,533,660 bytes with exact inventory equality and left a compatibility symlink; unrelated `/tmp` content remained untouched. Login `/tmp` now has 315,659,194,368 bytes free. The quota-aware measurement immediately before the A1 copy plus its exact payload implies approximately 587,163,312,420 bytes (546.84 GiB) shared headroom before concurrent writes. The relocation helper now requires an explicit destination tier after its redundant internal guard was found to mislabel this shared move as Tier T; the authoritative pre-copy Tier-S guard passed.

## 6. Upcoming experiments (order; ~60–70 node-days vs ~120 available)

1. **M3 seed 2 + M5** — continue the active fixed step-400 anchor run on an12 and seed-2 queue on an29; reconcile checkpoint lifecycles without early scientific reads. Two one-hour health windows are complete.
2. **M11** — let the reconciled watchdog finish the nine-cell InternVL matrix and backfill eight pending Gemma cells on reserved an29 capacity; then audit all 18 cells and publish the placement-aware final report.
3. **M6** — let the registered GPU-inert queue run CP/member one-step smokes sequentially after seed 2 and M11 release one full node, audit them, then merge a separate main-arm marker; corpus, 300-pair catch set, matched 120-step configs, advantage equivalence, and nondegenerate step-0 reward statistics are complete.
4. **M3 seed 3** — launch the third pilot seed after the higher-priority seed-2/M5/M11/M6 work is placed.
5. **M7/M9** — run the stratified ViRL 3B decomposition and four-arm, three-seed 7B flagship according to their frozen data/config readiness.
6. **M10** — execute the 64-sample follow-up for the 80 frozen M2 candidates, then fold the same check into later readouts.
7. **M12/M13** — fixed-size no-zoom v08 viewer is ready; Richard schedules human review after seed 1. Run TP4 strong-caption scoring when priority work releases four GPUs. Maintain paper slots only from landed, hash-pinned readouts.
8. **M14** — 7B CP merge-back readouts only after mini-A5 and flagship seed 1.

## 7. Open decisions and pre-committed forks

- PI-2 (GPT) ratification pending: prereg changes; Paper 1/2 charter; mini-A5 promotion; robustness arms; 7B arm set (A2-gray dropped at 7B); second-family list.
- **ViRL39K fork (pre-committed, decide by rule when L10 lands):** substantial blind reward-opportunity → thesis generalizes, run flagship as planned; genuinely visual corpus → reframe as "shortcut availability is a measurable corpus property," geo3k as demonstration, audit as the headline tool. Two-sided interpretation table goes into the prereg (optional change #5).
- Venue: ICLR 2027 preferred, not binding; scope is claim-driven, not deadline-driven.
- Human dependencies: R19 audit (now), R20 audit sample (this week).

## 8. Document maintenance protocol

Codex: after each ledger `pass`, append one row to §4 or update §5/§6 in the same commit; never edit §1–§3 or §7. PIs: decisions land as one line in §3 or §7 with date. This file never substitutes for the preregistration or the ledgers; on conflict, the merged prereg wins.
