# Main-Phase Deviations Log

- 2026-07-16 | M2 | The first readout config contained three incorrect immutable run-directory timestamps and failed before opening scientific rows; the corrected v2 manifest retained the audited hashes. The complete machine analysis then exposed a Markdown-only frozen category-identifier bug, so the machine JSON was preserved and Markdown was rendered from it after a fixture-backed identifier fix; no scientific value was recomputed.
- 2026-07-16 | M11 | The pre-existing scheduler used the fourth exact R19 marker as its only priority gate and launched eight lower-priority InternVL cells before M5/seed-2 placement. Two blind cells failed and six FlipTrack cells remain running; they were not terminated. Remaining M11 cells are held behind the PI-ordered M5/seed-2 work.
- 2026-07-16 | M3 | The first seed-2 capacity queue waited for the terminal M5 launch even though the integrity job had already reserved an12 and four GPUs were free on an29. It launched no training job and opened no performance value. The queue was terminated with SIGTERM, retained immutably as a failed/superseded orchestration run, and replaced by a fixture-backed queue that reserves the M5 node from the integrity state while allowing seed 2 on the other node.
- 2026-07-16 | M11 | The first two InternVL ViRL blind-sample cells failed before inference because an inference-only script imported input helpers through the pilot-reward module and the frozen M11 runtime intentionally lacks `mathruler`. The failed runs remain immutable. Commit `70444e7` splits the lightweight conditioning path with an adversarial import fixture; no package was added. Fresh real/no-image/caption runs use new directories on an12 GPUs 4/5/6.
- 2026-07-18 | M5/M12 | A 72B chart-v08 caption-store job was colocated on an12 GPUs 4–7 while M5 occupied GPUs 0–3. GPU allocations were disjoint, but shared host-memory use crossed Ray's 95% threshold and killed M5 after its verified step-150 checkpoint. The caption store completed and was audited; its ephemeral 146.8-GB checkout was deleted. M5 resumes from step 150 in a new immutable run and remains fixed to terminal step 400. The launcher now rejects 72B/RL host colocation.
- 2026-07-18 | M5 | Engineering OOM triage displayed partially redacted training-log rows for steps 152–156, including non-endpoint training fields. The values were not used for stopping, scheduling, or interpretation. Recovery uses the last hash-verified checkpoint at step 150 and the registered terminal rule remains unchanged.
