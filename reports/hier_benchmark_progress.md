# Hierarchical benchmark build — progress ledger (08-12 dispatch, P0–P2)

One line per dispatch task ID: `pass | fail | blocked | running` + note.
Honest `blocked` beats a thin `pass`. NOTE on IDs: the dispatch's P0.x/P1.x
IDs are scoped to THIS ledger only — they collide with differently-defined
IDs in `docs/EXPERIMENT_TODO.md` (its P0.1–P0.4 are the completed Phase-0
tasks; its P1.1 is the superseded cue-ladder rebuild). Mapping: dispatch
P0.1 ≈ TODO P1.1b; P1.0 ≈ HB.0; P1.1 ≈ HB.1+HB.3+HB.5; P1.2+P2.x ≈ HB.7.
Registration of record: `docs/registered_hier_benchmark_v1.md` (merged
2026-08-16, commit `2248c7f`).

| ID | Task | Status | Note |
|---|---|---|---|
| P0.1 | Verifier-operand audit, every generator with a verifier | pass | `reports/hb_p0_verifier_operand_audit_v1.md` — 6 exceptions recorded; the I21 gap in the premise-v2 re-checker FIXED (+ end-to-end fixture); sweep over 12 manifests / 3,740 rows: 0 problems; one cue-ladder-class fixture per generator (`tests/test_question_operand_audit.py`, 10 tests). |
| P0.2 | Retraction headers on `reports/cue_ladder_readout_v1.md` + RESULTS §16 | pass | Both headers applied 2026-08-16 (supersede, not delete); `.json` siblings covered by the md banner. Neither carried a retraction marker before (verified by grep). |
| P0.3 | premise-v2 branch-(c) n=5 + E2-failing types excluded | pass | Executed in the 2026-08-16 consolidation round: dev_v2 one-shot, E1 PASS (0.5125 in band, n=5 frozen); exclusions stand (E2 FAIL persists via the lenient-class collision — PI fork). `reports/track4_premise_v2_gate_readout_v2.*`. |
| P0.4 | Census exporter: standing, inventory-driven review artifact | pass | `scripts/build_generator_census.py` (+4 fixtures): every template-carrying `data/**.jsonl` appears automatically; first census `reports/generator_census_v2.{json,md}` — 50 families, 168 variants, 92 files scanned, 84 variants loudly stage-unmapped; arms-joining deferred to the P2.4 package builder by design. Census v3 (post-hier) under P2.4. |
| P1.0 | `docs/registered_hier_benchmark_v1.md` before any item exists | pass | Authored + merged 2026-08-16 (`2248c7f`), before any HB item existed. Chart crossing-density bands + pair-role split to be pinned by amendment BEFORE chart/coord generation (P1.2 gate). |
| P1.1 | Mother-item derivation in both generators; verifiers (a)–(e), each with an adversarial fixture | pass | `scripts/hier_v1_lib.py` + `scripts/build_hier_dev_batch.py` + `scripts/verify_hier_dev_batch.py`; offset-cue engine under the registered allowed-color ink rule; Amendments A1 (concrete grids/cue/probes) + A2 (layer × role matrix: target-switch is L3-only — a switch pair admits no single truthful identity question); 8 fixtures incl. cue-occlusion + unrendered-variant detection (`tests/test_hier_v1_builder.py`). |
| P1.2 | Dev batches: 150 mother-items per family per knob cell, one shot | pass | 7 cells × (150 L3 + 100 L2 + 100 L1 + 150 probe) rows, seed 20260817; coord balance ≤ 0.10 every cell; 9-series palette CIE76 minima 26.4 normal / 16.2–20.7 CVD (above the chart-v08 floors); candidate registries per causal L2/L3 manifest. From-disk verification **0 problems** (4,900 images rehashed, 1,400 cue pairs pixel-checked; `logs/verify_hier_dev_20260816.log`). Deviation: attempt 1 died at s9_low (random-values proposal cannot reach the registered low-crossing band at 9 series); partial tree removed pre-declaration; banded PROPOSAL added, registered band filter unchanged (`reports/hier_v1_dev_build_v1.json` deviations). |
| P2.1 | Base 3B/7B + `mini_a5_std_seed1`/`mini_a5_cp_seed1` step-120 on all layers/roles + discovery probe; open-form + candidate-ranking | pass | Open-form: all four 28-manifest sweeps complete rc=0 (an29 0–3, ~85 min each; run dirs `hier_p2_openform_*_20260816T2109*`). Ranking: all four 14-config sweeps complete rc=0 (an29 4–7, `20260816T212*`; base7b landed 2026-08-17T01:09Z). Readout `reports/hier_p2_ranking_readout_v1.{json,md}` (mean pair MRR / top-1): **L2 largely solvable** — chart L2 0.8332–1.0000 (base7b 1.0000 on s5_low), coord L2 0.6363–0.8857; **L3 drops sharply every cell** (base3b 0.4033–0.6395; base7b leads all seven L3 cells 0.5306–0.7965); Gate-1 step-120 checkpoints within ±0.08 MRR of base3b in every cell, both layers. Caveat (numbers): highest 3B L3 MRR sits in the attacker-flagged low-crossing cells (s5_low 0.6395, s9_low 0.5936 vs 0.4428/0.4033 high) — saliency of the band-breaking edit; resolved by the registered revision. NOTE: an12 4–7 launches were guard-denied — LH2's plain-text claims trip the m7 guard's fail-closed parse rule node-wide (the 08-15 failure class, reproducible; claim-format unification remains a PI-flagged fix). |
| P2.2 | HB.7 informativeness gates on base 3B, per knob cell | pass | `reports/hier_p2_gate_readout_v1.{json,md}` — monotone L1>L2>L3 in 6/7 cells; **family L3 floor PASS both families**; cells passing every cell gate: coord n8 (0.725/0.640/0.425), n12 (0.675/0.595/0.325); chart s9_low (0.795/0.745/0.200). Band FAILs as designed: s5_high monotone+L1 (0.495/0.535 — cue does not rescue dense 5-series crossings), s9_high L1 0.450, s5_low L2 0.880 (too easy), n20 L1 0.575. L3 switch reported separately (0.040–0.370). Numbers only; branch selection is PI work. |
| P2.3 | Blind-floor + 72B caption-stress cells + attacker checks | running | **Blind floors** (`reports/hier_p23_readout_v1.{json,md}`, base 3B): coord L3 0.1200/0.1133/0.1367 (n8/n12/n20; gray = no_image exactly), chart L3 0.0000 all four cells, every discovery probe 0.0000 — floors sit far under the real-image accuracies. **Attacker gates** (`reports/hier_p2_attacker_gate_*.json`, folded ≤0.55 point AND CI-up ≤0.62): `hier_coord_v1` gate **false** by one per-template point — dinov2 n20 folded 0.5577 (CI up 0.5897); pooled clean 0.5096/0.5096/0.5137 (dinov2/freq/meta). `hier_chart_v1` gate **false decisively** — pooled dinov2 0.6711 [0.6472, 0.6969], frequency_stat 0.6957 [0.6661, 0.7241], metadata 0.5910; per-template worst s5_low: frequency 0.9819 (CI up 0.9962), metadata 0.9315, dinov2 0.9190; s9_low 0.7831/0.8637/0.8103; s5_high/s9_high dinov2 ≈0.56. **Leak verification** (`scripts/verify_hier_leak_direction.py` → `reports/hier_p2_leak_verification_v1.json`): chart target_switch lowers the target's xa value in **200/200** pairs (unidirectional by construction); chart target_stable in low-crossing cells moves downward **99/100** (filter-induced) vs balanced 25/25 · 20/30 in high cells; PNG bytes — low-cell edited side larger **198/200**, mean +1.0–1.5 KB (the metadata/frequency signal); coord stable balanced, switch is a 2-D move (multi-field), n20 size skew 38/50 smaller (mean −148 B) matches its single marginal violation. Fix path is a **registered revision** (symmetrized switch edit + band-preserving low-cell stable edits) — PI-visible, not silent regeneration. **72B caption stress**: ModelScope ephemeral download → an29 `/dev/shm` running (`modelscope_ephemeral_qwen25vl72b_hier_p23_an29_20260817T002824Z`); detached chain on an29 (`logs/chains/hier_caption_stress_chain_20260817.sh`, log `logs/hier_caption_stress_chain_20260817T003947Z.log`) gates checkout → QA inputs (`build_hier_caption_stress_inputs.py`, file-suffix side binding + fixtures) → TP4 store (an29 0–3) → QA pairs → base-3B text QA (gpu0, guarded) → `build_hier_caption_stress_readout.py`. Deviations: first chain launch (log `…003723Z`) died at VM PTY close before detaching — relaunched node-side via the no-PTY `nohup setsid` idiom; an empty aborted download shell (`…002809Z`, jq absent from PATH on the first launcher call) removed. |
| P2.4 | Census review package v3 (new families) → human gates queue | pass | `reports/review_packages/hier_v1_census_v3{,.zip}` (zip sha256 `4b55781197ea399300653efe6a53d157a43870035106db2161d2fa753acb1530`; build report `reports/hier_census_review_package_v3.json`). Census v3 `reports/generator_census_v3.{json,md}`: 138 manifests, 51 families, 217 variants, 84 stage-unmapped — hier cells appear as 100-pair variants across L1/L2/L3 stages. Deterministic sample, R19/R20 discipline (no RNG): first 2 mother-pairs per (cell, role) — 42 pairs, 280 images, all existing layers joined by `mother_item_id` (switch ships L3-only per A2). `queue.md` human gates: chart-v08 no-zoom (Richard — blocks chart-side P2 freeze), hier_coord legibility + cue visibility, hier_chart review marked **diagnostic-only** pending the attacker-gate revision, census v3 coverage sign-off. Never self-certified. |

Post-P2 stop: per the dispatch, branch selection (band recalibration, chart
switch symmetrization, coord n20 template disposition) is PI work; this
ledger reports gate outcomes as numbers only.
